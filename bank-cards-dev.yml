build:
  version: "2.0"

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/bank-cards-db
    driverClassName:
    username: root
    password: root # NOT for production
  jpa:
    show-sql: true
  sql:
    init:
      mode: always

cards:
  message: "Bank cards DEV APIs "
  contactDetails:
    name: "Ahmad - Non-prod Support"
    email: "ahmad@bank.com"
  onCallSupport:
    - (089) 666-3216
    - (089) 666-3217